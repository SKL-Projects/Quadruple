import React from "react";
import Cost_list_insert from "../view/Cost_list_insert";

function Cost_insertContainer({ navigation }) {
   return <Cost_list_insert navigation={navigation} />;
}

export default Cost_insertContainer;
