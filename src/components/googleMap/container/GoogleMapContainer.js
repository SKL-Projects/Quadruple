import React from "react";
import GoogleMap from "../view/GoogleMap";

function GoogleMapContainer({ navigation }) {
   return <GoogleMap navigation={navigation} />;
}

export default GoogleMapContainer;
