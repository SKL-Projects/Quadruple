import React from "react";
import AutoCompleteContainer from "../components/googleMap/container/AutoCompleteContainer";

export default function GoogleMapScreen({ navigation }) {
   return <AutoCompleteContainer navigation={navigation} />;
}
