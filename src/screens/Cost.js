import React from "react";
import CostContainer from "../components/cost/container/CostContainer";

export default function GoogleMapScreen({ navigation }) {
   return <CostContainer navigation={navigation} />;
}
