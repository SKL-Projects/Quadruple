import React from "react";
import Cost_insertContainer from "../components/cost/container/Cost_insertContainer";

export default function Cost_insertScreen({ navigation }) {
   return <Cost_insertContainer navigation={navigation} />;
}
